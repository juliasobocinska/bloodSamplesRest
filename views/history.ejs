<%- include('partials/head') %>
<%- include('partials/nav') %>

<h1>Historia Zamówień</h1>

<table class="styled-table">
    <thead>
        <tr>
            <th>Wiek</th>
            <th>Ilość próbek</th>
            <th>Badania</th>
            <th>Data</th>
            <th>Akcja</th>
        </tr>
    </thead>
    <tbody>
        <% if (myOrders && myOrders.length > 0) { %>
            <% myOrders.forEach((z, index) => { %> 
                <tr>
                    <td><%= z.age %></td>
                    <td><%= z.quantity %></td>
                    <td><%= Array.isArray(z.tests) ? z.tests.join(', ') : z.tests %></td>
                    <td><%= new Date(z.data).toLocaleDateString() %></td>
                    <td>
                        <a href="/delete/<%= index %>" 
                           onclick="return confirm('Czy na pewno usunąć?')" 
                           class="action-link text-danger">
                           [ Usuń ]
                        </a>
                        
                        <a href="/edit/<%= index %>" class="action-link edit-link">
                           [ Edytuj ]
                        </a>
                    </td>
                </tr>
            <% }); %>
        <% } else { %>
            <tr>
                <td colspan="5">Brak zamówień w systemie.</td>
            </tr>
        <% } %>
    </tbody>
</table>

<%- include('partials/footer') %>
